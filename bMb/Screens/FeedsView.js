import React from 'react';
import {View, Text} from 'react-native';

const FeedsView = ({navigation}) => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Welcome To FeedsView!</Text>
    </View>
  );
};

export default FeedsView;
